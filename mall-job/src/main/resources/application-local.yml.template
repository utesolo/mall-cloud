# 本地开发配置模板 - 复制此文件为application-local.yml并填写真实配置

spring:
  data:
    redis:
      host: localhost
      port: 6379
      database: 3
      password: your-redis-password  # 如果Redis没有密码则删除此行

# Nacos配置
nacos:
  server-addr: localhost:8848

# 日志配置
logging:
  level:
    xyh.dp.mall: debug
    xyh.dp.mall.job: info
