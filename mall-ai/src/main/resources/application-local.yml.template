# 本地开发环境配置模板
# 使用方式：复制此文件并重命名为 application-local.yml，然后根据实际情况修改配置

# 本地Nacos地址（如果与默认不同）
nacos:
  server-addr: localhost:8848

# 本地数据库配置（通常在Nacos中配置，本地开发可在此覆盖）
# spring:
#   datasource:
#     url: jdbc:mysql://localhost:3306/mall_ai?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
#     username: root
#     password: your_password

# 本地Redis配置（通常在Nacos中配置，本地开发可在此覆盖）
# spring:
#   data:
#     redis:
#       host: localhost
#       port: 6379
#       database: 0

# 本地Spring AI配置（通常在Nacos中配置，本地开发可在此覆盖）
# spring:
#   ai:
#     dashscope:
#       api-key: your-dashscope-api-key
#       model: qwen-plus
#       embedding-model: text-embedding-v2
#     qdrant:
#       host: localhost
#       port: 6333
#       collection-name: mall_documents
